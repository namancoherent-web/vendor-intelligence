{
  "version": "1.1.2",
  "requirements_changed": false,
  "notes": "Fixed the update-checker's own lock mechanism: a stale lock file from an unclean shutdown could permanently block future update checks. Now checks the actual server port instead of a file."
}
