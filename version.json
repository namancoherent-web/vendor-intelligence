{
  "version": "1.2.10",
  "requirements_changed": false,
  "frontend_changed": false,
  "notes": "Verified the Indonesia/country-list fix from the last update with a full real test run - it worked correctly. Along the way, found and fixed two more real companies from Kenya (a .co.ke domain and a company whose site said it's based in Nairobi) that were slipping into a Europe-only report the same way - Kenya wasn't recognized yet either. Also double-checked the earlier concern about off-topic companies (unrelated software/tech firms) showing up in reports - a real test run confirmed the tool is already correctly filtering those out, so no change was needed there. Reminder: if a run still looks thin or off for a market, first check that your DEEPSEEK_API_KEY in .env is a valid, working key."
}
