{
  "version": "1.1.4",
  "requirements_changed": false,
  "frontend_changed": true,
  "notes": "Removed the 'How many companies?' (Focused/Standard/Broad) picker from Step 3 of the Web UI - every run now always uses the broad/most-thorough setting, with no user choice to reduce it. Web UI users: this needs a rebuild, applied automatically on the launch after the update downloads."
}
