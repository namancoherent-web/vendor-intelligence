{
  "version": "1.1.3",
  "requirements_changed": false,
  "frontend_changed": true,
  "notes": "Fixed confusing 'Not signed in' banner shown alongside a completed run's results in the Web UI. A 401 on downloads/preview now clearly says the session ended and to sign in again, instead of surfacing a raw error string next to an otherwise-working result card. Web UI users: this needs a rebuild, applied automatically on the launch after the update downloads."
}
