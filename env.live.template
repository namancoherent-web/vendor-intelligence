# Copy these lines into your .env for LIVE mode (fill keys marked PASTE)
# cp env.live.template .env  then edit .env

USE_MOCK_DATA=false

# --- REQUIRED: one LLM key (pick one provider) ---
LLM_PROVIDER=opencode
OPENCODE_API_KEY=PASTE_YOUR_OPENCODE_KEY
OPENCODE_MODEL=deepseek-v4-flash-free
# Or: LLM_PROVIDER=anthropic
# ANTHROPIC_API_KEY=PASTE_YOUR_ANTHROPIC_KEY

# --- Search: DDGS → Bing HTML → SearXNG (no paid API keys) ---
SKIP_DDGS=false
SKIP_BING_HTML=false
PREFER_SEARXNG=false
DDGS_BACKENDS=bing,mojeek

SEARXNG_BASE_URL=http://127.0.0.1:8080
MIN_RESULTS_BEFORE_BACKUP=3
RESULTS_PER_QUERY=25
# DDGS_TIMEOUT=20
# DDG_REQUEST_DELAY=true
# DDG_REQUEST_DELAY_MIN=2
# DDG_REQUEST_DELAY_MAX=4

# --- Enrichment ---
WIKIDATA_ENABLED=true
WEB_FETCH_ENABLED=true
SELENIUM_HEADLESS=true
SELENIUM_PAGE_LOAD_TIMEOUT=25
SELENIUM_IMPLICIT_WAIT=3
SELENIUM_SCROLL_PAUSE=0.6
GOOGLE_ALERTS_ENABLED=false

# --- Phase 3 fast validation ---
PHASE3_FAST_VALIDATION=true
PHASE3_PARALLEL_WORKERS=5
PHASE3_MAX_HITS_PER_QUERY=3
MAX_VALIDATION_ENTITIES=60
TARGET_SOLID_COMPANIES=30
DDG_NEWS_API=false
